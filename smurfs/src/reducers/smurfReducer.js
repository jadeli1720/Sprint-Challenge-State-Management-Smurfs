const initialState = {
    smurfs: [],
    test: "It must me working"
}

export const reducer = (state = initialState, action) => {
    switch(action.type) {
        default:
            return state;
    }
}